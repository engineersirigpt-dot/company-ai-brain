# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Generated output
qdrant_storage/
parsed_output/

# P2 Docker build runtime artifacts (build receipts/image ids — runtime output, ไม่ commit)
.p2_build/
p2_image.id
p2_build_manifest.json
wheels/

# Secrets
api.txt
.env
*.key
api_keys.json
api_keys_ISSUED.txt
api_keys.p5b.json
ingest_manifest.jsonl
permission_eval_raw.json

# Raw documents (ไม่ commit เอกสารบริษัท)
info/
*.pdf
*.xlsx
*.docx
*.pptx
*.ppt
*.xls
*.doc
*.zip
*.rar
*.7z
*.mp3
*.wav
*.m4a
*.exe
*.dll
*.bat

# Local scratch / intake dump (เอกสารดิบ, รูปสแกน, ข้อมูลลูกค้าจริง — ห้าม commit เด็ดขาด)
# หมายเหตุ: ext rules ด้านบนไม่ครอบ .jpg/.png/.mp4/.py/.db ที่อยู่ใน tmp/ → ignore ทั้งโฟลเดอร์
tmp/

# OS
.DS_Store
Thumbs.db

# real-run scratch output
.p2_m4_out/
